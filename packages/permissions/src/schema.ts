// Permission tables live in @consensus/db/src/schema/permissions.ts
// Re-exported here for convenience so callers can import from @consensus/permissions
export { permissionRules, permissionRequests } from '@consensus/db';
