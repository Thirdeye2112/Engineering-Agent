{
  "name": "@pilot-utils/tsconfig",
  "version": "1.0.0",
  "private": true,
  "description": "Shared TypeScript base configuration for the pilot-utils monorepo",
  "exports": {
    "./tsconfig.base.json": "./tsconfig.base.json"
  },
  "files": [
    "tsconfig.base.json"
  ],
  "sideEffects": false,
  "scripts": {
    "build": "echo 'No build step required for JSON config package'",
    "dev": "echo 'No dev step required for JSON config package'",
    "test": "echo 'No tests for this package'",
    "typecheck": "echo 'No typecheck step required for JSON config package'",
    "lint": "echo 'No lint step required for JSON config package'"
  }
}
